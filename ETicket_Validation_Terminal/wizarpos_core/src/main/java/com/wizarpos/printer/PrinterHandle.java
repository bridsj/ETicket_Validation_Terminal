package com.wizarpos.printer;

import android.content.Context;

import com.wizarpos.activity.DriverHandle;


public class PrinterHandle extends DriverHandle {
    @Override
    public void executeClickItemOperate(String command, Context context) {
        super.executeClickItemOperate(command, context);
    }
}
